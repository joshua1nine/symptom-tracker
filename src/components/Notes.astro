---
interface Props {
	label: string
	data: string
}

const { label, data } = Astro.props
---

<li class="flex flex-col gap-2">
	<span>{label}</span>
	<p class={`rounded-md border border-gray-300 bg-white p-4 text-sm`}>
		{data}
	</p>
</li>
